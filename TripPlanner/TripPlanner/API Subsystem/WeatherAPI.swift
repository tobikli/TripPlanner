//
//  WeatherAPI.swift
//  TripPlanner
//
//  Created by Tobias Klingenberg on 09.10.24.
//

import Foundation

/*
class WeatherAPI {
    ///Weather apiKey and URL for fetching Weather Data based on Location
    private let apiKey: String = ""
    private let baseURL: String = ""
    
    
    func fetchWeatherCaller(for location: String) {
        Task {
            return try await fetchWeather(for: location)
        }
    }
    
    ///Takes location as string and returns temperature, state, and validity
    func fetchWeather(for location: String) async throws -> (String, String, Bool) {
        guard let url = URL(string: baseURL + location) else {
            return ("", "", false)
        }
        return ("Test", "Test", true)
    }
}
*/
